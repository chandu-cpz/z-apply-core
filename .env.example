DEFAULT_USERNAME=
DEFAULT_PASSWORD=
TELEGRAM_BOT_TOKEN=
TELEGRAM_GROUP_CHAT_ID=
GMAIL_CREDENTIALS_PATH=credentials.json
GMAIL_TOKEN_PATH=token.json

# Model provider: "agnes", "inferx", or "nim". Leave empty to auto-detect
# from whichever provider API key is set.
MODEL_PROVIDER=

# Agnes AI (free) provider key.
AGNES_API_KEY=

# InferX hosted endpoint provider key and model override.
INFERX_API_KEY=
INFERX_MODEL=deepseek-v4-flash-0731
