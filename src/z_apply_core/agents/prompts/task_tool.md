<role>Launch one Z-Apply specialist and return its final message.</role>

<context>Dispatch exactly ONE task call per response — the runtime runs them concurrently.</context>

<task>Available specialist types:
{available_agents}</task>